#include "Engine/globals.h"

const SDL_Rect tile_size = {0, 0, TILE_SIZE, TILE_SIZE};
const SDL_Rect screen_size = {0, 0, SCREEN_WIDTH, SCREEN_HEIGHT};
const SDL_Rect scene_size = {0, 0, SCENE_WIDTH, SCENE_HEIGHT};